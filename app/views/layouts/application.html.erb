<!DOCTYPE html>
<html>
  <head>
    <title>ExploringOptions</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <div class="container-fluid px-0 px-sm-3 col-sm-12 border-bottom">
      <!-- NAVIGATION -->
      <nav class="navbar navbar-expand-sm d-flex px-2 py-0">
        <a href="/" class="navbar-brand abs">
          E<span class="d-none d-sm-inline">xploring</span>
          O<span class="d-none d-sm-inline">ptions</span>
          </a>
        <a class="navbar-toggler px-1 px-sm-2 py-2" type="" data-toggle="collapse" data-target="#navbarDropdown" aria-controls="navbarDropdown" aria-expanded="false" aria-label="Toggle Navigation" title="menu">
          <%= icon('fas', 'ellipsis-h') %>
        </a>
        <div id="navbarDropdown" class="navbar-collapse collapse">
          <% if current_user %>
            <ul class="navbar-nav">
              <li class="nav-item px-3 px-sm-1">
                <span class="d-inline d-sm-none">Timeout in: </span><%= tag.span current_user.timeout_in, class: "text-monospace",id: 'timeout-clock' %>
              </li>
            </ul>
          <% end %>
          <ul class="navbar-nav ml-auto p-0 d-flex justify-content-end">
            <% if current_user %>
              <li class="nav-item px-3 px-sm-1 py-1">
                <%= link_to destroy_user_session_path, method: :delete, class: 'nav-link text-dark', title: 'sign-out' do %>
                  <%= icon('fas', 'sign-out-alt') %>
                  <span class="d-none d-sm-inline">Sign Out</span>
                <% end %>
              </li>
              <li class="nav-item px-3 px-sm-1 py-1">
                <%= link_to profiles_path(current_user), class: "nav-link text-dark" do %>
                  <%= icon('fas', 'home', class: 'p-0 m-0') %>
                  <span class="d-inline-block d-md-none d-lg-inline-block">Dashbaord</span>
                <% end %>
              </li>
            <% else %>
              <li class="nav-item px-3 px-sm-1 py-1">
                <%= link_to new_user_session_path, class: 'nav-link text-dark' do %>
                  <%= icon('fas', 'sign-in-alt') %>
                  <span class="d-none d-sm-inline">Login</span>
                <% end %>
              </li>
              <% end %>
          </ul>
        </div>
      </nav>
    </div>
    <div class="container p-sm-1 pb-5">
      <% if flash.any? %>
      <!-- ERROR messages -->
      <div class="small alert alert-warning alert-dismissible fade show" role="alert">
        <button type="button" class="close" data-dismiss="alert" ><%= icon("fas", "times", class: 'fa-sm float-right', id: 'alert-close' ) %></button>
        <% flash.each do |name,msg| %>
          <% unless name == "timedout" %>
            <%= content_tag :div, id: "flash-alert", role:"alert" do %>
              <%= msg %>
            <% end %>
          <% end %>
        <% end %>
      </div>
      <% end %>
      <% current_user ? user_session["last_request_at"] : "not-logged-in" %>
      <%= yield %>
    </div>
  </body>
</html>
